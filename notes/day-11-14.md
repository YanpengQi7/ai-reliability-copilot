# Day 11–14 — Prompt v2 + versioned routing + UI switcher

See [week-2-findings.md](./week-2-findings.md) for the v1→v2 changelog and manual side-by-side observations.

## Done
- Prompt v2 targeting the 5 v1 failure modes
- `getSystemPrompt(version)` registry
- All 3 inference endpoints accept `version` (body field or query param)
- `prompt_version` column persisted on every analysis
- Home page: v1/v2 toggle (default v2)
- Detail page: re-run buttons for both versions side-by-side; history shows version badge

## Next (Week 3)
- Quantify "v2 > v1" with a rubric + LLM-as-judge eval pipeline
