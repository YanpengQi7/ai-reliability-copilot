# Day 22–30 — Packaging & portfolio

## Done
- **README.md** — replaced create-next-app boilerplate with full portfolio README: architecture diagram (ASCII), the 9-section schema, prompt eval methodology summary, scenario library table, local setup, known limitations, tech stack
- **.env.local.example** — committed (gitignore opt-in `!.env.local.example`) so contributors / interviewers can see what env to set
- **EVALUATION.md** (Week 3) — already shipped; serves as the methodology deep-dive linked from README
- **docs/blog-1-building-the-copilot.md** — story arc, architectural choices, what-I'd-do-differently
- **docs/blog-2-eval-is-the-product.md** — the core technical-marketing piece; this is the one to publish first
- **docs/portfolio.md** — resume bullets, two CARL stories ("vague problem → measurable" and "architecture call"), LinkedIn post, 30-second interview opener

## Deployment
- Repo: https://github.com/YanpengQi7/ai-reliability-copilot (public)
- Vercel auto-deploys from `main`; every day-N commit triggered a preview + prod deploy
- After this commit, the user should set DEEPSEEK_API_KEY + Supabase env vars in Vercel dashboard for the live demo to actually run

## What's deliberately not built (for honesty / interview points)
- Magic-link auth — adds friction for portfolio demo with no value; in EVALUATION.md and README listed as roadmap if multi-tenant became a goal
- Per-scenario repeats with stddev — listed as roadmap, would 3x API spend
- Cost-per-quality metric — would have been easy on Day 1; back-fill noted as "what I'd do differently" in blog #1
- Adversarial scenarios — single-cause incidents only for now; multi-cause + misleading-data scenarios are the obvious next eval expansion

## Final shape
- 30 git commits (one per day or day-range), each with a deliverable
- 9 application routes (4 pages + 5 API endpoints + 2 dynamic detail pages)
- 5 scenarios × 2 prompt versions = the live regression-test matrix
- 1 rubric, 1 judge, 1 dashboard

The story now writes itself: "I built an AI tool. The product is fine. The eval pipeline is the engineering."
