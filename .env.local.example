# DeepSeek API
DEEPSEEK_API_KEY=

# OpenAI API — used by the vision endpoint AND the cross-model judge
# (npm run evals:crossjudge, which measures same-family judge bias).
OPENAI_API_KEY=
# Override the independent cross judge (default: openai:gpt-4o-mini)
# JUDGE_MODEL_CROSS=openai:gpt-4.1-mini

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
